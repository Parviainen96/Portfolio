# Helppo asia monimutkaisesti tehtynä, turhien 
# välivaiheiden kautta. Pelkkä yksi kerta 
# for loopissa olisi riittänyt toteuttamaan 
# metodin toiminnallisuuden.

def range(n):
    lista = []
    luku = 0
    while n > 0:
        lista.append(n-1)
        n -= 1
    rlista = reversed(lista)
    l = []
    for i in rlista:
        l.append(i)
    return l
    
# Käytännössä siis näin:
def k(n):
    lista = []
    for i in range(0, n):
      lista.append(i)

    return lista
    
print(k(9))
